Drop an app.ico here (32x32 + 16x16 multi-resolution) before building, or the
resource compiler will warn about a missing IDI_NOTEPADL. Any valid .ico works
for M1; the final NotePad-L-style icon set lands in M4/M5 together with the
toolbar bitmaps.
