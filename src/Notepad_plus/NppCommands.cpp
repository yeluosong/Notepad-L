// NppCommands.cpp
//
// Reserved for the command handler table (M3+). In M1 command routing is
// done inline in Notepad_plus_Window::WndProc and edit commands forwarded via
// Notepad_plus::OnEdit().
